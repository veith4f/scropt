apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  labels:
    app.kubernetes.io/name: scropt
    app.kubernetes.io/managed-by: kustomize
  name: scropt-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: scropt-role
subjects:
- kind: ServiceAccount
  name: scropt
  namespace: system
